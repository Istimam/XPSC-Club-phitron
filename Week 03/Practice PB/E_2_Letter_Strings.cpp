#include<bits/stdc++.h>
#define For(i,A,B) for(int i = A; i < B; i++)
using namespace std;
void solve()
{
    int n;
    cin >> n;
    vector<string> vs(n);
    for (int i = 0; i < n;i++)
    {
        cin >> vs[i];
    }
    long long cnt = 0;
    for (int i = 0; i < n-1; i++)
    {
        int freq[26] = {0};
        
    }
    cout << cnt<<'\n';
}
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    
    return 0;
}